public class BasicVariableReference {

    public BasicVariableReference() {}

    public static int test() {
        int a = 2;
        boolean b = true;
        if (b) {
            int c = 3;
            int d = 123;
            boolean f = false;
            if (c + a - a + c - c == a*a/a) {
                return 1;
            } else if (c >= a && -1 < 0) {
                if (!f && c <= d && a < d && a != d && !false) {
                    return d;
                } else {
                    return 4;
                }
            } else {
                return 3;
            }
        } else {
            return 2;
        }
    }
}